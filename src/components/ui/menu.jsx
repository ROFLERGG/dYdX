import { useState } from "react"
import MenuButton from "./menu-button"

const Menu = () => {
  return (
    <div className={`fixed translate-x-full w-screen h-screen bg-primary`}></div>
  )
}

export default Menu